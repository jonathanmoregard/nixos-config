# .github/workflows/ci.yml
#
# CI workflow. Runs on `pull_request` (PR-branch code).
# Allowed: builds, tests, lint, eval.
# Forbidden: labelling, AI review, anything that gates merge — those
# live in gate.yml on `pull_request_target`.
#
# Trust boundary: this workflow's permissions are read-only for content;
# merge-gating logic lives in gate.yml (pull_request_target).
#
# Fork policy: this repo does NOT accept external contributions. Every
# job below is gated on either a push event OR a PR whose head lives in
# this same repository. Fork PRs cause every job to skip — no PR code
# from outside this repo runs in CI. close-fork-prs.yml handles the
# auto-close so fork PRs don't sit open silently.
#
# Runner: GitHub-hosted (ubuntu-latest). Public repo = unlimited free
# minutes. KVM nested-virt available since Feb 2023; verified via the
# DETERMINATE_NIX_KVM env exported by determinate-nix-action.
name: ci

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: pr-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  verify-fork-guards:
    name: verify fork-guards
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - run: scripts/check-fork-guards.sh

  flake-check:
    name: flake check (eval)
    runs-on: ubuntu-latest
    needs: verify-fork-guards
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: DeterminateSystems/determinate-nix-action@7c4cc317e802185875512bfcb68259257279d767 # v3
      - uses: nix-community/cache-nix-action@135667ec418502fa5a3598af6fb9eb733888ce6a # v6
        with:
          # Scope writes by ref so PR caches don't bleed across branches;
          # restore falls back to main-only to avoid cross-PR cache reuse.
          primary-key: nix-${{ runner.os }}-eval-${{ github.ref }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-eval-refs/heads/main-
          gc-max-store-size-linux: 5G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-eval-
          purge-last-accessed: 86400
          purge-primary-key: never
      # `nix flake check` evaluates darwinConfigurations.mac-mini even
       # without --all-systems (Determinate Nix walks all named outputs).
       # mac-mini is a placeholder (no users.users.jonathan declared,
       # home.homeDirectory hardcoded for Linux), so it fails to eval
       # on a Linux runner. Probe Linux outputs explicitly instead.
       # Re-enable `nix flake check` once mac-mini's actual config lands.
      - run: |
          set -euo pipefail
          nix eval --no-warn-dirty .#nixosConfigurations.dellan.config.system.build.toplevel.drvPath > /dev/null
          nix eval --no-warn-dirty .#nixosConfigurations.vm.config.system.build.toplevel.drvPath > /dev/null
          nix eval --no-warn-dirty .#checks.x86_64-linux.dellan-vm.drvPath > /dev/null

  build-dellan:
    name: build dellan toplevel
    runs-on: ubuntu-latest
    needs: flake-check
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: wimpysworld/nothing-but-nix@687c797a730352432950c707ab493fcc951818d7 # v10
      - uses: DeterminateSystems/determinate-nix-action@7c4cc317e802185875512bfcb68259257279d767 # v3
      - uses: nix-community/cache-nix-action@135667ec418502fa5a3598af6fb9eb733888ce6a # v6
        with:
          primary-key: nix-${{ runner.os }}-dellan-${{ github.ref }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-dellan-refs/heads/main-
          gc-max-store-size-linux: 5G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-dellan-
          purge-last-accessed: 86400
          purge-primary-key: never
      - run: |
          nix build .#nixosConfigurations.dellan.config.system.build.toplevel \
            --no-link --print-build-logs --keep-going

  vm-minimal:
    name: vm-minimal
    runs-on: ubuntu-latest
    needs: build-dellan
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: read
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        lane: [1, 2, 3]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: wimpysworld/nothing-but-nix@687c797a730352432950c707ab493fcc951818d7 # v10
      - uses: DeterminateSystems/determinate-nix-action@7c4cc317e802185875512bfcb68259257279d767 # v3
      - uses: nix-community/cache-nix-action@135667ec418502fa5a3598af6fb9eb733888ce6a # v6
        with:
          primary-key: nix-${{ runner.os }}-vm-${{ github.ref }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-vm-refs/heads/main-
          gc-max-store-size-linux: 5G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-vm-
          purge-last-accessed: 86400
          purge-primary-key: never
      - name: Verify KVM
        run: |
          test "${DETERMINATE_NIX_KVM:-}" = "1" || { echo "KVM not available"; exit 1; }
          ls -la /dev/kvm
      - run: nix build .#checks.x86_64-linux.dellan-vm -L

  vm-graphical:
    name: vm-graphical
    runs-on: ubuntu-latest
    needs: build-dellan
    permissions:
      contents: read
    if: |
      (github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository) &&
      (contains(github.event.pull_request.changed_files, 'home/cinnamon.nix') ||
       contains(github.event.pull_request.changed_files, 'modules/nixos/desktop.nix') ||
       contains(github.event.pull_request.changed_files, 'home/kitty.nix'))
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - run: |
          # Placeholder: VM-graphical check derivation TBD (spec E.1).
          # Path-conditional; runs only when desktop-affecting files change.
          echo "vm-graphical: not yet implemented (spec E.1)"
