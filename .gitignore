# Nix build results
result
result-*

# direnv
.direnv/

# VM disk images (don't commit these)
*.qcow2
*.vmdk

# Editor
.DS_Store

# Claude-skills runtime state (per-host, regenerated by skill manager)
/.agents/
/.claude/
/skills-lock.json
