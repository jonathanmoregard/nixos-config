# Nix build results
result
result-*

# direnv
.direnv/

# VM disk images (don't commit these)
*.qcow2
*.vmdk

# Editor
.DS_Store

# Agent session scaffolding (mission briefs / session constraints).
# Per-worktree working notes, not config — kept out of the repo so a
# `git add -A` before a flake build can't drag them into a PR.
/tasks/

# Claude-skills runtime state (per-host, regenerated by skill manager)
/.agents/
/.claude/
/skills-lock.json
